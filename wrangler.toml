# Cloudflare Pages Configuration for Kainowpag
# This file configures the deployment settings

name = "kainowpag"
compatibility_date = "2025-09-27"

[site]
bucket = "./build/web"

# Serve static files from build/web directory
[[assets]]
binding = "ASSETS"
directory = "./build/web"
